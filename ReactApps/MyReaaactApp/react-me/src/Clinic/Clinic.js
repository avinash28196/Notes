import React from 'react';

const clinic = (props) => {
    return  <div>
            <p> Clinic name is: {props.name} and Doctor Name is: {props.doc} </p>
            </div>
};

export default clinic;